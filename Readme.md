# LINE GIRL - แพลตฟอร์มสั่งอาหารออนไลน์ 🍔

![Next.js](https://img.shields.io/badge/Next.js-15-blue)
![React](https://img.shields.io/badge/React-19-blue)
![Tailwind CSS](https://img.shields.io/badge/Tailwind_CSS-3.3-blue)
![MySQL](https://img.shields.io/badge/MySQL-8.0-blue)
![License](https://img.shields.io/badge/License-MIT-green)

LINE GIRL คือเว็บแอปพลิเคชันสำหรับสั่งอาหารออนไลน์เต็มรูปแบบที่พัฒนาด้วย **Next.js** และ **React 19**  
ออกแบบมาเพื่อเชื่อมต่อผู้ใช้งาน 3 กลุ่มหลัก:

- **Customer (ลูกค้า)** – ค้นหาและสั่งอาหาร  
- **Shop (ร้านค้า)** – จัดการร้านค้าและเมนูของตนเอง  
- **Admin (ผู้ดูแลระบบ)** – จัดการผู้ใช้และข้อมูลระบบทั้งหมด  

---

## 🚀 ฟีเจอร์หลัก

### 👤 Customer (ลูกค้า)
- สมัครสมาชิก / ล็อกอิน  
- ค้นหาร้านค้าและฟิลเตอร์ตามประเภท, ระยะทาง, เรตติ้ง  
- ดูเมนูและรายละเอียดสินค้า (`/page.jsx`)  
- ตะกร้าสินค้า (เพิ่ม/ลด/ลบ) ผ่าน React Context  
- ยืนยันคำสั่งซื้อและเลือกวิธีชำระเงิน  
- เขียนรีวิวและให้คะแนน (หลังการสั่งซื้อสำเร็จ)  

![Customer UI](public/uploads/ภาพถ่ายหน้าจอ%202568-11-02%20เวลา%2022.09.01.png)

---

### 🏪 Shop (ร้านค้า)
- สมัครสมาชิก / ล็อกอิน (สำหรับ Shop)  
- Onboarding สำหรับร้านค้าใหม่ (`/manage/create`)  
- จัดการร้าน: ชื่อ, รูปโปรไฟล์, ที่อยู่, ตำแหน่ง Lat/Lng  
- จัดการเมนู (CRUD) พร้อมอัปโหลดรูปภาพ  
- ลบรูปภาพเมนูออกจาก Server อัตโนมัติเมื่อเมนูถูกลบ (`/route.js`)  

![Shop UI](public/uploads/ภาพถ่ายหน้าจอ%202568-11-02%20เวลา%2022.10.39.png)

---

### 🔐 Admin (ผู้ดูแลระบบ)
- ล็อกอินสำหรับ Admin  
- จัดการผู้ใช้ทั้งหมด (CRUD: Customer, Shop, Admin) (`/route.js`)  
- จัดการแบนเนอร์หน้าแรก (อัปโหลด/ลบ)  
- ป้องกันการลบ Admin ของตัวเอง (`/route.js`)  
- ป้องกันการลบผู้ใช้ที่ยังมีข้อมูลค้างอยู่ (เช่น ร้านค้า, ออเดอร์) (`/route.js`)  

![Admin UI](public/uploads/ภาพถ่ายหน้าจอ%202568-11-02%20เวลา%2022.11.59.png)

---

## 💿 (สำคัญ) การติดตั้งสำหรับส่งงาน ITDS242

เพื่อให้สอดคล้องกับข้อกำหนดของรายวิชา โปรเจกต์นี้ถูก **จำลองการแยกส่วน** Front-end และ Back-end ออกจากกัน

**กรุณารันโปรเจกต์โดยใช้ 2 Terminals ดังนี้:**

---



### 1. การตั้งค่าฐานข้อมูลและ Environment (ทำครั้งแรก) 🗄️

1.  **เปิดโปรแกรม MySQL** (เช่น XAMPP, MySQL Workbench) และ Start Server
2.  **สร้าง Database ใหม่:**
    * เปิดโปรแกรม (เช่น phpMyAdmin) และสร้าง Database ใหม่
    * แนะนำให้ใช้ชื่อ `linegirl` (เพื่อให้ตรงกับค่าเริ่มต้นในไฟล์ Environment)
3.  **Import ฐานข้อมูล:**
    * ใน Database ที่เพิ่งสร้าง, คลิกแท็บ **"Import" (นำเข้า)**
    * เลือกไฟล์ **`secx_gry_database.sql`** (ที่อยู่ใน Root ของ Repository นี้)
    * กด "Import" หรือ "Go" เพื่อรันสคริปต์ (ระบบจะสร้างตารางและ `INSERT` ข้อมูลเริ่มต้นทั้งหมด)
4.  **ตั้งค่า Environment (สำคัญมาก!):**
    * **เปิดไฟล์ `.env.local` ทั้ง 2 ไฟล์** ทั้ง **`sec2_gry_fe_src`** และ  **`sec2_gry_ws_src`**  แก้ไขข้อมูลให้ตรงกับ MySQL Server ของคุณ:
      ```bash
      # MySQL Database Credentials
      DB_HOST=localhost
      DB_USER=[กรอก User ของคุณ เช่น root]
      DB_PASSWORD=[กรอกรหัสผ่าน MySQL ของคุณ ]
      DB_DATABASE=linegirl # (ต้องตรงกับชื่อ Database ที่คุณสร้างในข้อ 2)

      # JWT Secret Key (สำหรับสร้าง Token)
      JWT_SECRET=Linegirl-webapplication
      ```

---



### 2. รันโปรเจกต์ (ต้องใช้ 2 Terminal)

**💻 Terminal ที่ 1 (รัน Back-end Web Service):**
```bash
# 1. เข้าไปที่โฟลเดอร์ Back-end
cd secx_gry_ws_src

# 2. ติดตั้ง Dependencies (ทำครั้งแรก)
npm install

# 3. รัน Server (จะทำงานที่ Port 3001)
npm run dev
```


**💻 Terminal ที่ 2 (รัน Front-end):**
```bash
# 1. เข้าไปที่โฟลเดอร์ Front-end
cd secx_gry_fe_src

# 2. ติดตั้ง Dependencies (ทำครั้งแรก)
npm install

# 3. รัน Server (จะทำงานที่ Port 8080)
npm run dev
```


**🧪 Test API สำหรับตรวจสอบการทำงาน**
ในโปรเจกต์นี้ มี API หลักที่ใช้ทดสอบระบบฝั่ง Back-end ดังนี้:
🔐 Admin API
📁 ไฟล์: app/api/admin/users/route.js
ใช้ทดสอบการจัดการผู้ใช้ (Customer, Shop, Admin) — เช่น การดึงข้อมูลผู้ใช้ทั้งหมด, การสร้าง, อัปเดต หรือการลบ

**🍴 Restaurant (Shop) API**
📁 ไฟล์: app/api/manage/menus/route.js
ใช้ทดสอบการจัดการเมนูร้านอาหาร เช่น เพิ่มเมนู, แก้ไขเมนู, ลบเมนู หรืออัปโหลดรูปภาพ